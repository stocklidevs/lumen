# Preact SSR on lumen

Server-side renders a Preact component with `preact-render-to-string`, served over `node:http`
(ESM).

```sh
npm install
../../target/release/lumen-cli server.mjs   # http://localhost:3000
```
