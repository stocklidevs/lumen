# h3 on lumen

The [h3](https://h3.unjs.io) HTTP framework, served through `node:http` via `toNodeListener` —
routing, path params, query parsing, and JSON body reading, all running unmodified.

```sh
npm install
../../target/release/lumen-cli server.mjs   # http://localhost:3000
```
